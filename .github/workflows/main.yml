name: 'one and two'

on: push

jobs: 
  run-test-one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: './actions/test-1.yml'
  
  run-test-two:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: './actions/test-2.yml'
    
