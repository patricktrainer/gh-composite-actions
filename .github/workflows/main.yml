name: 'one and two'

on: push

jobs: 
  run-test-one:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: .github/actions/test-one
  
  run-test-two:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: .github/actions/test-two
    
